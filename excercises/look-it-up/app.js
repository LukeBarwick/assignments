function storeData(word, definition) {
    return {
        Word: word,
        Definition: definition,
    }
}




var library = storeData("Run", "move at a speed faster than a walk, never having both or all the feet on the ground at the same time.");
console.log(library);
